class PlanModel {
  String? title, startDate, endDate, source, destination, people, budget;

  PlanModel(this.title, this.startDate, this.endDate, this.source,
      this.destination, this.people, this.budget);
}
